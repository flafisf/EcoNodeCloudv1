#compilar la aplicacion
npm run tsc

# Lanzarlo
node build/app/app.js

# probar aplicaci√n
links http://localhost:3000 o http://172.17.0.3:3000

# run directamente typescript (solo para desarrollo)
npm install ts-node-dev -s

# modificar package.json en "scripts" para typescript directo
"dev": "ts-node-dev --respawn --trasnpileOnly ./app/app.ts"
"prod":"tsc && node ./build/app.js"
